# Deployment

Reserved for declarative Kubernetes and home-automation configuration in RLS-06 and RLS-07.

Before adding manifests, capture the target cluster's storage class, ingress/DNS, secret handling,
namespace, broker exposure, and GitOps conventions. Never commit real credentials or deploy from an
agent session without explicit authorization.
