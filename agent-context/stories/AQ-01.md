# AQ-01 — AtmosMesh Aqua bring-up: SHT41 + genuine 128×64 OLED + duty-cycled water probe

- **Status:** In flight — software implemented and build/test-verified; hardware wiring blocked on
  acceptance criterion 1 (photo evidence) and independent review before any flash.
- **Priority:** P2
- **Milestone:** Aqua-M1 — Hardware approved (new; mirrors RLS-01/V15-01 for this third product)
- **Depends on:** ADR-0001 (product composition pattern), D-002/D-010/D-015 (raw-value labeling
  and single-ADC-channel discipline), D-019/ADR-0002 (MQTT-only transport for this variant),
  D-020 (Aqua OLED page layout)

## User story

As the operator bringing up new hardware, I want a new AtmosMesh product composition root for the
ESP8266MOD + SHT41 + 128×64 OLED + water-probe board, so that it gets the same reviewed,
host-tested, ID-based contract as AtmosMesh v1 and Grove v1.5 without duplicating or destabilizing
either.

## Outcome

A new, independently buildable product (`atmosmesh-aqua-v1`) publishes raw SHT41 temperature/
humidity and a raw, explicitly-uncalibrated water-probe ADC diagnostic over the existing MQTT
contract, on a genuine 128×64 local display, without touching AtmosMesh v1 or Grove v1.5.

## Product identity (three distinct values, per ADR-0001/D-011)

| Field | Value |
| --- | --- |
| Product name | AtmosMesh Aqua (proposed; operator names the product, as with D-008) |
| Stable product ID | `atmosmesh-aqua-v1` |
| Product variant | `esp8266-aqua-station` |
| Default station ID | `atmosmesh-aqua-0001` |
| Composition root | `firmware/src/products/atmosmesh_aqua_v1.cpp` |
| Canonical environment | `atmosmesh-aqua-v1` |

## In scope

- New `ProductProfile` entry with I2C bus on `D2`/GPIO4 (SDA) and `D1`/GPIO5 (SCL) — the ESP8266
  Arduino core's default `Wire` pins, chosen to avoid GPIO0/GPIO2's boot-strap hazard that D-009
  had to document and work around for Grove.
- SHT41 on that bus at I2C address `0x44` (Sensirion default).
- A genuine SSD1306-class 128×64 panel on the same bus, address preference `0x3C` then `0x3D`
  (D-001/D-006 convention). Reuse AtmosMesh v1's existing 128×64 U8g2 constructor/composition-
  boundary code rather than inventing a second 128×64 constructor path — the "small compile-time
  adapter at a composition boundary" ADR-0001 permits.
- A new `water_power_control_gpio` profile field on `D5`/GPIO14 (free — DHT is dropped) driving a
  high-side PNP switch mirroring D-015's proven pattern (2N3906, emitter=3V3, collector=probe VCC,
  base through ~2.2 kΩ, external 100 kΩ base-emitter pull-up, active LOW, fail-safe HIGH/OFF
  established before `pinMode(OUTPUT)`). This is mandatory, not optional — a wetted probe corrodes
  at least as readily as YL-38's soil contacts, so D-015's duty-cycling argument applies with equal
  or greater force.
- Do not overload the existing `soil_power_control_gpio` field name for a water sensor — add a
  distinctly-named field. AtmosMesh v1 and Grove v1.5 leave it at the existing `-1` sentinel, same
  as they already do for fields they don't use.
- Water probe AOUT → `A0` (ESP8266's single ADC channel), reusing D-015's 30 s start-to-start /
  100 ms settle / bounded-averaged-sample cadence as a starting default, not final until the
  probe's actual output range is confirmed (see Open questions). Reported as `water_adc_raw` only
  — never `water_level`, never a percent, never "moisture."
- New `MqttProductKind::AtmosMeshAquaV1` contract: topics `home/air/atmosmesh-aqua-0001/state` and
  `.../availability`, discovery node `atmosmesh_aqua_0001`, three discovery entities
  (`temperature_c`, `humidity_pct`, `water_adc_raw`) — no pressure entity (SHT41 does not measure
  pressure; do not carry Grove's `pressure_hpa` field into this product).
- Grove v1.5's soil sensor pattern is **not** reused by this product. The single ADC channel is
  dedicated entirely to the water probe. Grove v1.5's own composition root, profile, and soil
  sampler are unchanged.
- A new OLED page-layout decision (new decision number, not an amendment of D-016 — D-016 is bound
  to Grove's 128×32 geometry and does not extend to other products). Default proposal: reuse a
  4-row descriptive layout (temperature, humidity, water raw ADC, one spare/status row), taking
  advantage of the doubled vertical resolution for a larger, more legible font rather than packing
  8 thin rows — pin this down in acceptance criteria, not by assumption.

## Out of scope

- DHT11/BMP180 hardware or drivers (SHT41 replaces them for this product only).
- Reusing Grove's soil sensor physically or its `soil_adc_raw` field name.
- A status LED (D-014-style) — not requested for this variant; may be a later story.
- Moisture/water-level calibration, percentage, or any derived "level" claim.
- ESPHome native API transport — declined by D-019/ADR-0002 for this cycle.
- Any change to AtmosMesh v1 or Grove v1.5 pins/runtime/behavior.

## Open questions this story does not close (hard gate before wiring/flashing)

1. **Board identity.** "ESP8266MOD" is the silkscreen on the ESP-12E/12F module, not a specific
   board design. It could be a bare module breakout with no onboard USB-serial/regulator/boot-
   strap resistors, or a NodeMCU-style devboard (what Grove v1.5 already is). Do not assume —
   photograph and confirm before wiring, exactly as D-009 did for Grove. The pin table above
   assumes NodeMCU-style as the likely case, matching the operator's other hardware, but is not
   binding until confirmed.
2. **Water probe identity.** A 3-pin VCC/GND/AOUT device with no DO pin is probably not a YL-38 —
   YL-38 is a 4-pin comparator carrier (VCC/GND/DO/AO) built around an LM393 comparator with a
   trim pot. Check for an LM393 chip and trimpot on the probe board. If absent, it is more likely a
   bare resistive probe strip whose "AOUT" is a raw divider node — a different electrical model
   with a different dry/wet voltage range. D-010's rule still binds regardless of which it is:
   confirm the exact board's onboard A0 divider (if any) and the probe's dry/wet AOUT range at
   3.3 V before connecting anything to A0. Do not copy Grove's divider without re-deriving it for
   this specific probe and board.
3. Final 30 s/100 ms/sample-count cadence values are provisional until (2) is resolved — a
   different probe's electrical/corrosion characteristics may justify different numbers.

## Acceptance criteria

- [ ] Given the physical board and probe are unidentified, when this story starts, then the first
  artifact produced is photographed evidence resolving open questions 1 and 2 above, reviewed the
  same way D-009's board probe was — no wiring proceeds before this.
- [x] Given the confirmed board, when the product profile is added, then
  `firmware/include/atmosmesh/product_profile.hpp` gains an Aqua profile with
  `product_id="atmosmesh-aqua-v1"`, `product_variant="esp8266-aqua-station"`,
  `station_id="atmosmesh-aqua-0001"` as three distinct strings, `i2c_sda_gpio=4`,
  `i2c_scl_gpio=5`, `oled_width_px=128`, `oled_height_px=64`, a new
  `water_power_control_gpio=14`, and `dht_data_gpio`/`light_rc_gpio`/`status_led_*_gpio`/
  `soil_power_control_gpio` left at `-1` sentinels. Implemented; host-tested in
  `test_product_variants.cpp`. Pin *values* are still provisional pending open question 1.
- [x] Given the SHT41 is wired on the shared I2C bus, when firmware reads it, then
  `temperature_c` and `humidity_pct` are published only when a valid SHT41 frame is read, and are
  reported as unavailable — never numeric zero — otherwise (mirrors D-016's missing-value rule).
  Implemented (`sht4x_frame.hpp/cpp`, CRC-verified per Sensirion's datasheet vector) and
  host-tested; not yet exercised against a real sensor.
- [x] Given the 128×64 panel is wired, when the live page renders, then it reuses AtmosMesh v1's
  existing 128×64 U8g2 constructor/adapter path (not a new duplicated constructor), and the
  row-count/font choice is recorded as a new decision, distinct from D-016. Implemented
  (`products/atmosmesh_aqua_v1.cpp` uses `U8G2_SSD1306_128X64_ALT0_F_HW_I2C`, same as v1); layout
  recorded as [D-020](../decisions.md).
- [x] Given the water probe's PNP switch is wired, when firmware boots, then the base control
  GPIO is driven HIGH (OFF) before `pinMode(OUTPUT)`, exactly mirroring D-015's fail-safe
  sequencing. Implemented via direct reuse of `soil_sampler.hpp/cpp` (not forked/genericized, per
  AC below) — Grove's tested state machine, not a new one.
- [x] Given a completed sample cycle, when the value is published/displayed, then it appears only
  as `water_adc_raw` — no percent, no "level," no "moisture," no calibration claim — and is
  omitted (not zero) before the first completed sample. Implemented and host-tested
  (`test_aqua_state_omits_missing_readings_and_never_carries_pressure`); a raw zero reading is
  still published (`test_aqua_water_raw_zero_is_shown_not_hidden`).
- [x] Given the new MQTT contract, when discovery is published, then it uses `atmosmesh_aqua_0001`
  as the discovery node and exactly three entities (temperature, humidity, water_adc_raw raw
  diagnostic), with no pressure entity and no ESPHome transport code (per D-019/ADR-0002).
  Implemented (`mqtt_contract.hpp/cpp`, `MqttProductKind::AtmosMeshAquaV1`) and host-tested,
  including a `<768` byte payload-size assertion matching `PubSubClient::setBufferSize(768)`.
- [x] Given the full repository, when this story's changes are built, then `task test`, the new
  canonical `atmosmesh-aqua-v1` build, and all existing canonical/compatibility builds
  (`atmosmesh-v1`, `atmosmesh-grove-v1_5`, `esp32dev`, `esp8266-grove`) stay green, and Grove
  v1.5's and AtmosMesh v1's own behavior/pins are provably untouched. Verified 2026-08-26: 109/109
  native tests pass, `task check` clean, all five hardware/compat environments build clean. Grove's
  **pins, GPIO map, and every Grove-specific test assertion are unchanged.** Grove's *binary size*
  is not identical: measured against a pre-change worktree at the same commit, RAM 34324→34900
  bytes (41.9%→42.6%, +576 B) and flash 303927→306463 bytes (29.1%→29.3%, +2536 B). Cause: Grove
  links the shared `mqtt_contract.cpp`, which now also carries Aqua's discovery specs/contract
  literals — the same pre-existing pattern already puts Grove's own contract data inside the v1
  ESP32 build. Both products' pin/behavior contracts stay untouched; only the shared translation
  unit's static data grew, well inside Grove's RAM/flash budget.
- [ ] Given software acceptance is complete, when hardware flashing is proposed, then independent
  review of the head commit precedes any flash command, matching the ADR-0001/V15-06 pattern.

## Validation

- **Automated:** Failing host tests first for profile field values/sentinels, SHT41 reading
  validity/invalidation, water-probe sampler timing (reuse/extend the soil sampler's tested state
  machine shape), new discovery entity count/labels, and the "no pressure entity" contract. Then
  `task test`, new canonical build, all existing canonical/compatibility builds, `task check`.
- **Manual:** Photograph board/probe identity before wiring (open questions 1–2). Bench-run all
  sensors together, confirm OLED renders all four rows without truncation, confirm MQTT discovery
  appears correctly in Home Assistant.
- **Failure/edge case:** SHT41 read failure shows "unavailable," not zero. Water probe stays
  powered off except during its sample window. No flash without independent review.

## Evidence

- 2026-08-26: `pio test -e native` → 109/109 passed (includes new `test_sht41_frame.cpp`,
  `test_aqua_status.cpp`, Aqua-profile assertions in `test_product_variants.cpp`, and five new
  Aqua-contract tests in `test_mqtt_contract.cpp`).
- 2026-08-26: `pio run -e atmosmesh-aqua-v1` → SUCCESS, RAM 42.1% / Flash 28.9% (comparable to
  Grove's, well inside budget with no ESPHome stack per D-019/ADR-0002).
  `pio run -e atmosmesh-v1 -e atmosmesh-grove-v1_5 -e esp32dev -e esp8266-grove` → all SUCCESS.
  Grove's own pins/behavior/tests are unchanged; its binary grew slightly (RAM +576 B, flash
  +2536 B against a measured pre-change baseline) because it links the shared `mqtt_contract.cpp`,
  which now also carries Aqua's contract data — see the acceptance-criteria note above.
- 2026-08-26: `task check` (whitespace/conflict-marker check) → clean.
- 2026-08-26: esptool `chip_id`/`flash_id` against the connected ESP8266MOD over USB-serial →
  genuine ESP8266EX, 26 MHz crystal, WiFi feature present, 4 MB flash — board is electrically
  healthy (does not by itself resolve open question 1, board-model identity).
- No hardware sample, MQTT discovery capture, or OLED photo yet — nothing has been flashed.

## Notes

- ESP8266 board and water probe hardware identity are still unconfirmed as of 2026-08-26.
  Acceptance criterion 1 (photo evidence) is the only thing blocking wiring; independent review is
  the only thing blocking flashing. All other acceptance criteria are implemented and verified at
  the software/build level above.
- **Before soldering the water probe to A0:** check whether the board has the two small resistors
  near the A0 pin (NodeMCU-style header divider, 0–3.3 V input) or whether A0 is the bare
  ESP-12E/12F module's TOUT (0–1.0 V max). A 3.3 V-referenced probe wired straight to a bare
  module's A0 can destroy the ADC. I2C (SHT41 + OLED) is safe to solder either way. This was
  communicated to the operator at implementation start and is not yet confirmed.
- Companion transport decision: [D-019](../decisions.md) /
  [ADR-0002](../../docs/adr/0002-mqtt-vs-esphome-native-api-transport.md) — MQTT-only, ESPHome
  native API declined for this cycle. OLED layout: [D-020](../decisions.md).
