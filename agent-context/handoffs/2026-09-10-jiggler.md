# JIG-01 handoff — 2026-09-10

Standalone BLE jiggler in worktrees/atmosmesh/ble-jiggler, branch feat/ble-jiggler.
User confirmed attached board; esptool identifies ESP32-C3 revision 0.4, embedded 4MB flash.
Original complete flash read to /tmp/atmosmesh-jiggler-before.bin (4194304 bytes).

Native tests 180/180; C3 build 513908 bytes flash, 24804 RAM. Review round 1 R1 (untested BLE
safety/report policy) addressed through JigglerHid extraction and four tests; fresh review pending.
Firmware not yet flashed. Target Mac pairing, OLED visual status and button behavior pending.

Scope: tiny +1/-1 horizontal relative counts each 15 seconds, no buttons/keyboard in HID map.
BOOT GPIO9 toggles after normal boot; RESET restarts OFF. External switch GPIO3 to GND optional.
No MQTT first iteration; user minimum prioritized. Use separate root and build allowlist.

Tooling: PlatformIO absent PATH on Linux; installed pinned 6.1.19 separately in workspace
.tooling/python/platformio (not esptool venv). Add its bin to PATH for tasks. Esptool remains
scripts/esp-tool with .tooling/python/atmosmesh. Default sandbox hid USB and network; full-access
permission then enabled USB read and dependency install. No git config changed.

Retrospective: readiness gating, disconnect latch and failed-notify reset are application policy,
so test them outside hardware composition. Existing Spot no-pin U8g2 construction reused to avoid
I2C pin ownership issue. No harness changes proposed.


Update: final independent APPROVE at 65ff145, 181/181 tests. Firmware flashed successfully;
startup serial `jiggler: ready oled=ok button=GPIO3 boot=GPIO9 mode=off`. BLE advertising verified
as AtmosMesh Jiggler, HID service 0x1812, appearance 0x03c2 input-mouse. PR #16 open, not merged.
Mac pairing/movement and visible OLED/button acceptance requested from operator.

CI: all PR #16 checks green (native push/PR and CodeQL). No merge performed.

Operator confirmed Mac connected (2026-09-10). BOOT toggle, visible motion and idle prevention still awaiting confirmation.

Correction: operator reports Mac could NOT properly pair. Previous connection confirmation withdrawn. Diagnose before accepting hardware.

Pairing-fix 2026-09-11 (`6990ed7` on feat/ble-jiggler / PR #16):
Symptoms from operator: OLED stayed on "BT pairing..."; Mac Bluetooth briefly disabled then
re-enabled. Linux bluetoothctl pair also failed with BlueZ `device_confirm_passkey` (numeric
comparison) despite Just Works IO cap.

Change: HID report map now declares three unused buttons + relative X/Y (hosts reject a
buttonless mouse); PnP ID 0x02/0x303A/0x6A01; bonding without Secure Connections; ENC+ID key
distribution; serial logs for connect/auth/subscribe/disconnect.

Flashed 2026-09-11 after merge to `6ea2387`. Operator confirmed Mac pairing works.
