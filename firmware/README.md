# Firmware

Reserved for the ESP32 implementation after RLS-01 and decision OQ-001.

Do not add a framework scaffold merely to make this directory look populated. The first firmware
change must record the selected toolchain, exact board target, reproducible build command, and a
minimal hardware-independent test or configuration validation path.
