# Host-side ESP32 tools. Import name for pyserial is `serial`.
# Install into a venv, not with system python:
#   task bootstrap-agent-python
#   # or: python3.12 -m venv .venv && .venv/bin/pip install -r firmware/requirements-agent.txt
pyserial==3.5
esptool==5.3.1
